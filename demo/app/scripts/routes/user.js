/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var UserRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return UserRouter;
});
